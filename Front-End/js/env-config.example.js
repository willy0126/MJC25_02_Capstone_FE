/**
 * 환경변수 설정 예제 파일
 *
 * 사용 방법:
 * 1. 이 파일을 js폴더 안에 'env-config.js'로 복사하세요
 * 2. 아래의 값들을 실제 API 키로 교체하세요
 * 3. env-config.js는 .gitignore에 포함되어 있어 Git에 커밋되지 않습니다
 */

window.ENV = {
  // 도서관 정보나루 API 키
  // https://www.data4library.kr/에서 발급받으세요
  OPENLIBRARY_KEY: 'YOUR_API_KEY_HERE'
};
